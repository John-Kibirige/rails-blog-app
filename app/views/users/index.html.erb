<ul>
    <% @users.each do |user| %>
        <li>
            <div>
                <%= image_tag user.photo, alt: "#{user.name}-avatar" %>
            </div>
            <div>
                <h3><%= user.name %></h3>
                <span >Number of posts: <%= user.posts_counter %></span>
            </div>
            <br>
        </li>

    <% end %>
</ul>
