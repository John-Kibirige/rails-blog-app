<h4>A list of all users </h4>
<ul>
    <% @users.each do |user| %>
        <li>
           <%= link_to user_path(user) do %>
                <%= render "shared/user", user: user %>
           <% end %>

        </li>
    <% end %>
</ul>
